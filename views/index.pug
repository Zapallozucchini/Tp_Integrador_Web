
extends layout.pug

block content
  h1 Sistema Hospitalario
  
  if dbStatus
    p Estado de la base de datos: 
      strong= dbStatus
    
    //- Verificar usando texto en lugar de emoji
    if dbStatus.includes('✅') || dbStatus.includes('exitos')
      p ¡Todo funciona correctamente!
      p Ahora puedes continuar con el desarrollo.
    else
      p.alert-error Por favor verifica la configuración de MySQL
  else
    p.alert-error No se pudo determinar el estado de la base de datos
    p Por favor revisa los logs del servidor

